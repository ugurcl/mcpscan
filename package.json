{
  "name": "mcpscan",
  "version": "0.1.0",
  "description": "Security scanner for MCP servers: detects tool poisoning, hidden instructions, and over-broad capabilities.",
  "type": "module",
  "bin": {
    "mcpscan": "dist/cli.js"
  },
  "main": "dist/scan.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/checks.test.ts",
    "scan": "tsx src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
